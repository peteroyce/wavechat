{
  "name": "wavechat",
  "version": "0.1.0",
  "description": "Minimal, self-hostable real-time chat. WebSocket rooms, presence indicators, typing signals, message history — no Firebase, no vendor lock-in.",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client",
    "test": "npm run test --workspace=server && npm run test --workspace=client",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
